<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>{{#if title}}{{title}} · {{/if}}Tutoring Scheduler</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <link href="/public/css/styles.css" rel="stylesheet" />
</head>
<body>
  <header class="site-header">
    <div class="container">
      <h1 class="brand">TUTORING SCHEDULER</h1>
      <nav>
        <a href="/">HOME</a>
        <a href="/my/sessions">MY SESSIONS</a>
        <a href="/tutor/slots">TUTOR</a>
        <a href="/admin/overview">ADMIN</a>
        <a href="/availability">AVAILABLE SLOTS</a>
        {{#if user}}
          <span class="badge">Hi, {{user.name}} ({{user.role}})</span>
          <a class="badge" href="/auth/logout">LOGOUT</a>
        {{else}}
          <a href="/auth/login">LOGIN</a>
        {{/if}}
      </nav>
    </div>
  </header>

  <main class="container">
    {{{body}}}
  </main>

  <footer class="site-footer">
    <div class="container">
      <small>© {{year}} Campus</small>
    </div>
  </footer>
</body>
</html>
